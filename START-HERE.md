# 🚀 TeamPulse Turbo - Швидкий Старт

## ✅ Перевірено та виправлено

Всі критичні помилки виправлені. Проєкт готовий до роботи!

## 📋 Що було зроблено

1. ✅ Створено `.env` файл з необхідними змінними оточення
2. ✅ Перевірено підключення до PostgreSQL
3. ✅ Створено базу даних `teampulse`
4. ✅ Перевірено всі файли структури проєкту
5. ✅ Протестовано запуск сервера
6. ✅ Перевірено всі JS та CSS залежності

## 🔧 Налаштування перед запуском

### 1. Встановіть OpenAI API ключ

Відредагуйте файл `.env` та замініть placeholder на ваш реальний ключ:

```bash
OPENAI_API_KEY=sk-proj-your-real-key-here
```

Отримати ключ можна тут: https://platform.openai.com/api-keys

### 2. Перевірте PostgreSQL

Переконайтесь що PostgreSQL запущений:

```bash
brew services list | grep postgresql
```

Якщо не запущений:

```bash
brew services start postgresql@15
```

## 🚀 Запуск проєкту

### Development режим:

```bash
npm run dev
```

### Production режим:

```bash
npm start
```

### З PM2 (для production):

```bash
npm run prod
```

## 🌐 Доступ до застосунку

Після запуску:

- **Головна сторінка**: http://localhost:3000
- **Health check**: http://localhost:3000/health
- **API**: http://localhost:3000/api/v1/

### Вхід:

- **Username**: `janeDVDops`
- **Password**: `jane2210`

## 📊 Корисні команди

### Перевірка статусу:

```bash
# Health check
curl http://localhost:3000/health

# Ping
curl http://localhost:3000/ping

# API info
curl http://localhost:3000/api/v1/info
```

### Зупинка сервера:

Якщо порт зайнятий:

```bash
lsof -ti:3000 | xargs kill -9
```

### Логи PM2:

```bash
npm run logs
```

## 🗄️ База даних

### Підключення до бази:

```bash
/usr/local/Cellar/postgresql@15/15.14/bin/psql -U postgres teampulse
```

### Очистка тестових даних:

```bash
curl -X POST http://localhost:3000/api/admin/cleanup-database \
  -H "Content-Type: application/json" \
  -H "Cookie: auth=authorized" \
  -d '{"confirmCode": "CLEANUP_TEST_DATA_2024"}'
```

## 📝 Структура проєкту

```
├── server.js              # Головний файл сервера
├── package.json           # Залежності
├── .env                   # Змінні оточення (створено)
├── utils/
│   ├── db.js             # PostgreSQL підключення
│   ├── logger.js         # Логування
│   └── openAIClient.js   # OpenAI інтеграція
├── routes/
│   ├── analyze.js        # Аналіз переговорів
│   ├── clients.js        # Управління клієнтами
│   ├── prospects.js      # Prospects management
│   ├── teams.js          # Team management
│   ├── negotiations.js   # Negotiation tracking
│   ├── advice.js         # AI advice
│   ├── audit.js          # Audit logs
│   └── best-hire.js      # Hiring module
├── middleware/
│   ├── rateLimiter.js    # Rate limiting
│   ├── validators.js     # Validation middleware
│   └── requestContext.js # Request context
└── public/
    ├── index.html        # Головна сторінка
    ├── login.html        # Сторінка логіну
    ├── modals.html       # Modal windows
    └── *.js, *.css       # Frontend assets
```

## ⚠️ Важливі примітки

1. **OpenAI API Key**: Без валідного ключа функції AI аналізу не будуть працювати
2. **PostgreSQL**: Має бути запущений перед стартом сервера
3. **Порт 3000**: Переконайтесь що порт вільний
4. **Node.js**: Потрібна версія >= 18.0.0

## 🔍 Troubleshooting

### Помилка: "EADDRINUSE: address already in use"

```bash
lsof -ti:3000 | xargs kill -9
```

### Помилка: "Cannot connect to database"

Перевірте що PostgreSQL запущений:

```bash
brew services restart postgresql@15
```

### Помилка: "Invalid OpenAI API key"

Оновіть `.env` з валідним ключем (починається з `sk-`)

## 📚 Додаткова документація

- `QUICKSTART.md` - Швидкий старт з прикладами
- `STARTUP-GUIDE.md` - Детальний гайд по запуску
- `FIXES-SUMMARY.md` - Список виправлених помилок
- `API-SPEC.md` - API документація
- `IMPLEMENTATION.md` - Технічна документація

## 🎯 Наступні кроки

1. Встановіть валідний OpenAI API ключ в `.env`
2. Запустіть сервер: `npm run dev`
3. Відкрийте http://localhost:3000
4. Увійдіть з credentials вище
5. Почніть працювати!

---

**Все готово до роботи! 🎉**

Якщо виникнуть питання - перевірте health endpoint або логи.
